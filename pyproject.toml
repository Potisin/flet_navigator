[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "flet-navigator"
version = "2.8.7"
description = "Flet Navigator with custom modifications"
dependencies = []
